_format_version: '3.0'

services:
  - name: users-api-service
    host: 'users-api' # Nome do serviço no Render
    port: 10000 # Porta interna que o users-api escuta (do log: 🚀 HTTP Server USERS running on port 10000!)
    routes:
      - name: users-register-route
        paths:
          - /users
        methods: ['POST']
        strip_path: false

      - name: users-authenticate-route
        paths:
          - /users/authenticate
        methods: ['POST']
        strip_path: false

  - name: urls-api-service
    host: 'urls-api' # Nome do serviço no Render
    port: 10000 # Porta interna que o urls-api escuta (presumindo que também use 10000)
    routes:
      - name: urls-click-route
        paths:
          - /
        methods: ['GET']
        strip_path: false

      - name: urls-operations-routes
        paths:
          - /urls
        methods: ['GET', 'POST', 'PATCH', 'DELETE']
        strip_path: false
